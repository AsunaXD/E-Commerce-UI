import 'package:flutter/material.dart';

class PrdouctScreen extends StatelessWidget {
  const PrdouctScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
